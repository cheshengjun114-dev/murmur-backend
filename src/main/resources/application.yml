spring:
  datasource:
    url: jdbc:mysql://localhost:3306/murmur?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: your-super-secret-key-must-be-at-least-32-characters